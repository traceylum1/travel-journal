---
description: Do not expose secrets or real credentials
alwaysApply: true
---

# Secrets

- Never paste real API keys, production URLs with credentials, or full `.env` contents into chat, rules, or commits.
- Use placeholders in examples; treat `.env` and compose secrets as sensitive and gitignored unless explicitly public.
